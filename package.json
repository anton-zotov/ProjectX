{
  "name": "projectx",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p .",
    "test": "node --no-warnings --test --test-isolation=none tests/guarantees.test.mjs tests/input.test.mjs",
    "serve": "node devtools/serve.mjs dist 5173",
    "preflight": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
